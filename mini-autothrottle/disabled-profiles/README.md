# Disabled profiles

These files are kept for reference and the loader will not use them.

- The A320 work was never finished or tested. It uses `D:`, which clashes with the firmware's `D,...` layout command.
- The 777 profile was never used or tested in this project.

Do not move either back into the active profile folder without revisiting the protocol and testing it with real hardware.
